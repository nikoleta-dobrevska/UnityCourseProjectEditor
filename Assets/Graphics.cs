using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Graphics : MonoBehaviour
{
    public static void Blit(object value, RenderTexture rt, Material s_MaterialSpline, int pass)
    {
        throw new NotImplementedException();
    }

    public static void Blit(RenderTexture source, RenderTexture rt)
    {
        throw new NotImplementedException();
    }

    public static void DrawTexture(Rect m_MonitorAreaRect, RenderTexture m_VectorscopeTexture)
    {
        throw new NotImplementedException();
    }

    internal static void DrawTexture(Rect rect, RenderTexture renderTexture, Material aMaterial)
    {
        throw new NotImplementedException();
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
